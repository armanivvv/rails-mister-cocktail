<%= cl_image_tag('pa14ymgnpylkx7tth9bp',
      width: 400, height: 300, crop: :fill) %>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient, collection: Ingredient.all %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>

